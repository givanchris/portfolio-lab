services:
  - type: web
    name: portfolio-lab
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run portfolio_lab.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    envVars:
      - key: HOLDINGS_FILE
        value: holdings_demo.json
